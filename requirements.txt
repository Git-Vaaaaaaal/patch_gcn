# PyTorch : fourni par "module load pytorch-gpu/py3/2.4.0" (torch 2.4.0 + CUDA 12.4.1).
# NE PAS installer torch via pip -r (déjà fourni par le module). torchvision doit
# être installé séparément avec --no-deps pour ne pas laisser pip réinstaller torch :
#   pip install --no-deps torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu124

# PyTorch Geometric : NE PAS installer via ce fichier (torch_sparse n'a pas de wheel
# précompilée pour toutes les combinaisons torch/CUDA et se remet à compiler depuis les
# sources, ce qui échoue car "import torch" n'est pas visible dans l'environnement de
# build isolé de pip). Installer séparément avec les wheels précompilées PyG :
#   pip install torch_geometric
#   pip install torch_sparse -f https://data.pyg.org/whl/torch-2.4.0+cu124.html
#  pip install torch_scatter -f https://data.pyg.org/whl/torch-2.4.0+cu124.html
# reste des dépendances utilisées par le code
numpy
pandas
scipy
scikit-learn
scikit-survival
lifelines
tensorboardX
h5py
matplotlib
